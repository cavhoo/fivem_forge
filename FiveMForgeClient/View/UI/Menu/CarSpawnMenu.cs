extern alias CFX;
using CFX::CitizenFX.Core;
using CFX::CitizenFX.Core.UI;
using NativeUI;

namespace FiveMForgeClient.View.UI.Menu
{
    public class CarSpawnMenu: UIMenu
    {
        public CarSpawnMenu(string title, string subtitle, bool glare = false) : base(title, subtitle, glare)
        {
            Initialize();
        }

        private void Initialize()
        {
            
        }
    }
}
