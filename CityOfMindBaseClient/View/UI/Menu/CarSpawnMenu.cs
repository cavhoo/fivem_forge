extern alias CFX;
using CFX::CitizenFX.Core;
using CFX::CitizenFX.Core.UI;
using LemonUI.Menus;

namespace FiveMForgeClient.View.UI.Menu
{
    public class CarSpawnMenu: NativeMenu
    {
        public CarSpawnMenu(string title, string subtitle) : base(title, subtitle)
        {
            Initialize();
        }

        private void Initialize()
        {
            
        }
    }
}
